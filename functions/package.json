{
  "name": "echo-reminder-functions",
  "description": "Echo app scheduled email reminders via Firebase Functions and SendGrid",
  "engines": {
    "node": ">=18"
  },
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "lint": "echo 'no linter configured'",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1"
  }
}
